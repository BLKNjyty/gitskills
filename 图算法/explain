*本文的图结构是用三个类表示Graph，Node,Edge
*拓扑排序是针对有向图的排序问题。

kruskal算法和prim算法收拾针对无向图的：计算最小生成树(保证图的联通性并且联通之后所有边的权值是最小的)
*K算法：
先加上权值最小的边，如果没有形成环则加上；...一下加下去，直到形成环位置

唯一的难点是：怎么判断有没有形成环

方法：各个节点单独形成各自的集合，先找最小的边，边的from和to是不是在一个集合中，如果不在那么不会形成环，则from和to合并成一个集合；如此反复进行下去，直到所有的边计算完毕。------就是并查集结构！
*p算法：
从点的角度出发，随便选一个节点加入集合，从当前节点出发，选择最小权值的边，此时集合又增加了一个节点；

再从集合中的节点能达到的边中选择权值最最小的，相应节点加入集合...依次进行下去；
